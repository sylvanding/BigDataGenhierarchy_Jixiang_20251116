如果您要为项目贡献代码，请提交PullRequest给我们。请注意您的代码要符合以下规范。
1. 类、函数、循环和判断后的‘{’在同一行，切勿另起一行；‘}’另起一行，不可同一行。
```java
public static ArrayList<BinInfo> completeOneDHistogram(double start, double width,
            double[] dist, int first, int last) {
        TreeMap<Integer, BinInfo> map = new TreeMap<Integer, BinInfo>();
        for (int i = first; i < last; i++) {
            double temp = (dist[i] - start) / width;
            int offset = (Math.abs(temp - Math.ceil(temp)) <= Delta) ? (int) Math.ceil(temp)
                    : (int) Math.floor(temp);
            if (map.containsKey(offset)) {
                map.put(offset, map.get(offset).addPoint(dist[i]));
            } else
                map.put(offset, (new BinInfo(start + (offset + 0.5) * width, width))
                        .addPoint(dist[i]));
        }

        ArrayList<BinInfo> result = new ArrayList<BinInfo>(map.size());
        result.addAll(map.values());
        return result;
    }
```

2. 类名和函数名请采用驼峰命名法。
```java
public class MckoiObjectIOManager implements ObjectIOManager{
    ......
    public Object readObject(final long pointer) {
        //todo
    }
    ......
}
```

3. 输出调试信息请使用Debug类的debug常量，日志请使用标准Logger打印。
```java
Logger logger = Logger.getLogger("index");
if (Debug.debug)
{
    logger.finer("Partitioning the " + j
            + "th cluster, size=" + clusterSize
            + ", Distances: ");
    for (int temp = clusterOffset[j]; temp < clusterOffset[j + 1]; temp++)
        logger.finer(wrapper[temp].getDouble() + ", ");
    logger.finer("");
}
```